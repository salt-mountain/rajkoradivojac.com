---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Email confirmed — Rajko Radivojac">
  <main class="min-h-screen flex items-center justify-center px-4 bg-navy-950">
    <div
      class="max-w-md w-full text-center bg-navy-800 rounded-lg shadow-2xl p-10"
    >
      <svg
        class="w-16 h-16 text-green-500 mx-auto mb-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <h1 class="font-serif text-3xl font-bold text-white mb-3">
        Email confirmed!
      </h1>
      <p id="confirmed-message" class="text-gray-400 leading-relaxed mb-8">
        Thanks for confirming. If you requested an excerpt, it's on its way to
        your inbox. You'll also get occasional updates on new releases.
      </p>
      <a
        href="/"
        class="inline-flex items-center justify-center px-6 py-3 bg-primary text-navy-950 font-semibold uppercase tracking-wider text-sm rounded hover:bg-accent transition-colors duration-200"
      >
        Back to the site
      </a>
    </div>
  </main>
</Layout>

<script>
  // Static page: personalize from the ?book query param at runtime if present.
  const book = new URLSearchParams(window.location.search).get("book");
  const el = document.getElementById("confirmed-message");
  if (book && el) {
    el.textContent =
      "Thanks for confirming — your free excerpt is on its way to your inbox. You'll also get occasional updates on new releases.";
  }
</script>
