{
  "name": "rajko-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK app: SES email infrastructure for rajkoradivojac.com",
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --outputs-file cdk-outputs.json",
    "audit-dns": "bun scripts/audit-dns.ts",
    "apply-dns": "bun scripts/apply-dns.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "aws-cdk": "^2.1128.0",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.259.0",
    "constructs": "^10.4.2"
  }
}
